# tao-counsel — SPEC

도덕경 81장을 근거 데이터로 두고, 고민을 들으면 관련 장을 찾아 해석해주고
실천까지 제안하는 Claude Code 하네스.

## 1. 문제와 목표

사람이 고민을 말했을 때 "도덕경 관점에서 이렇게 보세요"를 받고 싶다.
그냥 LLM에게 물으면 두 가지가 깨진다.

1. **인용이 가짜다.** 모델이 기억으로 도덕경 구절을 지어낸다. 그럴싸하지만
   원문에 없는 문장이 나온다.
2. **매번 스타일이 다르다.** 어떤 날은 점집 같고 어떤 날은 위키 요약이다.

그래서 이 하네스의 목표는 두 개다.

- **G1. 근거 있는 인용** — 상담에서 나가는 도덕경 문장은 100% `text/`
  파일에서 읽은 것. 기억에서 생성 금지.
- **G2. 재현되는 상담 형식** — 같은 고민이면 같은 구조의 답이 나온다.

### 비목표 (Non-goals)
- 웹/앱 UI. 터미널의 Claude Code 세션이 전부다.
- 도덕경 학술 번역 논쟁. 번역본은 하나를 정해 쓰고 출처를 밝힌다.
- 실제 심리 상담 대체. 위기 상황은 상담 대신 전문기관 안내로 빠진다.

## 2. 사용자 시나리오

```
사용자: 팀장이 자꾸 내 일에 끼어들어서 미치겠어
  ↓
[1] 고민을 주제 축으로 분해   → 통제욕 / 인간관계 / 무력감
  ↓
[2] text/ 에서 관련 장 검색   → 17장(태상하지유지), 22장(부쟁), 8장(상선약수)
  ↓
[3] 2~3장 선정 + 원문 인용    → 파일에서 그대로 읽어옴
  ↓
[4] 현자 해석                 → 고민의 언어로 풀이
  ↓
[5] 실천 제안 1~3개           → 내일 당장 할 수 있는 크기
  ↓
sessions/2026-07-25-....md 에 기록
```

## 3. 데이터 설계 (`text/`)

장당 파일 하나. `text/01.md` ~ `text/81.md`. 한 파일에 네 섹션.

```markdown
---
chapter: 8
themes: [물, 다투지 않음, 낮은 자리]
situations: [경쟁이 버거울 때, 인정받고 싶을 때, 지고 싶지 않을 때]
source: <저본 약칭, 예: gongyu-2019>
---

## 원문
上善若水 水善利萬物而不爭
處衆人之所惡 故幾於道

## 번역
가장 좋은 것은 물과 같다. 물은 만물을 이롭게 하면서도 다투지 않고,
뭇 사람이 싫어하는 낮은 곳에 머문다. 그러므로 도에 가깝다.

## 현대어
잘 사는 건 물처럼 사는 거다. 물은 다 먹여 살리면서도 "내가 했다"고
싸우지 않는다. 다들 피하는 아래쪽으로 그냥 흐른다. 그게 도에 가깝다.

## 주해
<이 장이 사람 사는 문제로 무엇을 말하는지. 마지막에 **오독 주의** —
이 장을 어떤 사람에게 쓰면 해가 되는지 한 줄.>
```

기준 샘플: `text/08.md`. 새 장을 쓸 때 이 파일의 밀도와 문체를 따른다.

**필드 이유**
- `themes` — 이 장이 다루는 추상 개념. 장끼리 묶는 축.
- `situations` — **검색의 핵심.** 사람은 "무위가 뭐죠"라고 묻지 않고
  "일을 놓기가 무서워요"라고 말한다. `situations`는 고민의 언어로 쓴
  일상 문장이어야 하고, `themes`처럼 한자어로 쓰면 검색이 죽는다.
- `source` — 이 장의 번역이 어느 저본에서 왔는지. 저본을 섞게 될 경우
  장별로 다를 수 있어 파일에 둔다.

### 섹션의 성격 — 인용과 파생물의 분리

네 섹션은 성격이 다르고, 이 차이가 테스트(§5)와 스킬(§4)을 결정한다.

| 섹션 | 성격 | 수정 | 출처 |
|---|---|---|---|
| 원문 | 불변 사실 | ❌ | 고전 |
| 번역 | **남의 저작물 인용** | ❌ | 저본 번역자 명시 |
| 현대어 | **우리가 쓴 파생물** | ✅ 계속 다듬음 | 우리 |
| 주해 | 우리가 쓴 해석 재료 | ✅ | 우리 |

`번역`이 있는데 `현대어`를 또 두는 이유: 저본 번역은 정확하지만 문어체라
고민 상담에 그대로 붙지 않는다("뭇 사람이 싫어하는 낮은 곳"). 현대어가
원문과 사용자의 말 사이를 잇는 다리다.

**파일을 나누지 않는 이유**: 한 장을 다룰 때 네 섹션을 항상 같이 본다.
`text/`·`modern/`으로 디렉터리를 쪼개면 Read가 늘고, 한쪽만 고쳐 어긋나는
사고가 반드시 난다. 붙어 있으면 눈에 보인다.

**검색 방식**: 프론트매터를 grep. 임베딩·벡터DB 없음. 81개는 전부 읽어도
컨텍스트에 들어가는 크기라 인덱스를 만들 이유가 없다. (실측 필요 — §7)

**출처**: 저본을 `text/SOURCE.md`에 명시 — 번역자, 판본, 라이선스 근거.
라이선스가 불명확한 번역본은 쓰지 않는다. → **열린 항목 O1**

## 4. 상담 스킬 (`.claude/skills/counsel/`)

`SKILL.md` 한 장. 아래 6단계를 강제한다.

1. **듣기** — 고민을 되묻지 말고 먼저 받는다. 정보가 정말 부족할 때만 1개 질문.
2. **분해** — 고민을 2~3개 주제 축으로 쪼갠다. (사용자에게 보여줌)
3. **검색** — `text/*.md` 프론트매터에서 `situations`/`themes` 매칭.
   **최소 5장 후보를 뽑고 2~3장으로 좁힌다.** 첫 히트로 끝내지 않는다.
4. **인용** — 선정된 장 파일을 Read로 열어 `원문`·`번역` 섹션을 **복사**한다.
   Read 없이 인용하는 것은 금지.
5. **해석** — 장별로: 이 구절이 당신 상황에서 무엇을 말하는가. `현대어`를
   재료로 삼아 사용자가 쓴 단어에 붙여 푼다.
6. **실천** — 1~3개. "무위하라" 같은 추상 금지. "내일 회의에서 먼저 말하지
   않기" 크기여야 한다.

### 섹션별 사용 규칙

- `원문` + `번역` → **인용 블록에만.** 한 글자도 바꾸지 않고, 출처를 밝힌다.
- `현대어` → **해석 본문에 녹여서.** 그대로 붙여넣지 않고, 사용자의 말투와
  상황에 맞춰 다시 쓴다.
- `주해` → **사용자에게 보이지 않는다.** 해석을 만드는 재료로만 쓴다.
  단 `오독 주의`는 반드시 확인한다 — 걸리면 그 장을 쓰지 않는다.

이 구분이 무너지면 무엇이 도덕경이고 무엇이 우리 말인지 사용자가 알 수
없게 된다. 인용 블록은 좁고 정확하게, 해석은 자유롭게.

**안전 규칙**: 자해·자살·학대 신호가 보이면 6단계를 중단하고 전문기관
안내로 전환한다. 도덕경 해석을 얹지 않는다.

**기록**: `sessions/YYYY-MM-DD-<slug>.md` — 고민 요약 / 인용한 장 번호 /
제안한 실천. 인용은 `[8장 번역]` 처럼 **장 번호와 섹션을 함께** 적는다
(T1의 대조 대상이 되므로 필수).

## 5. 검증 (`tests/`)

G1을 사람 눈으로 지킬 수 없으니 스크립트로 지킨다.

- **T1. 인용 대조** — 세션 기록의 `[N장 섹션]` 인용문이 해당 장 파일의 해당
  섹션에 실제로 있는지 문자열 대조. 없으면 실패. 이 프로젝트의 유일한 필수
  테스트다.
  - 대조 대상: `원문`, `번역` 만.
  - 제외: `현대어`, `주해` — 우리가 쓴 파생물이라 계속 다듬는다. 대조하면
    문장을 손볼 때마다 과거 세션 기록이 전부 실패로 뜬다.
  - 따라서 세션 기록에 `현대어`를 인용 블록으로 남기는 것 자체가 규칙 위반.
    T1은 `[N장 현대어]` 표기가 보이면 실패시킨다.
- **T2. 데이터 완결성** — `text/01..81.md` 전부 존재, 프론트매터 4필드
  (`chapter`/`themes`/`situations`/`source`) 존재, `situations`
  비어있지 않음, 네 섹션(`원문`/`번역`/`현대어`/`주해`) 모두 존재.

의존성 0 (node 또는 python 표준 라이브러리)으로 쓴다.

## 6. 작업 순서

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| M1 | `text/08.md` 샘플 1장(네 섹션 다) + `SOURCE.md` | 형식·현대어 문체 확정 |
| M2 | `counsel` 스킬 초안 | 1장만으로 상담 1회 굴려봄 |
| M3 | **형식 재검토** | 검색이 실제로 되는지 판단, 필요시 M1 형식 수정 |
| M4 | 나머지 80장 채우기 | T2 통과 |
| M5 | `tests/` T1·T2 | T1 통과 |
| M6 | 실사용 5세션 | 답변 품질 확인 |

M2·M3이 이 순서의 이유다. 81장을 다 채운 뒤 형식을 갈아엎으면
전량 재작업이라, 1장으로 먼저 검증하고 넘어간다.

## 7. 열린 항목

- ~~**O1. 번역 저본**~~ → **결정 (2026-07-25)**: 위키문헌 「번역:도덕경」
  (CC BY-SA 3.0/GFDL 1.2, 81장 완역). 저장소 전체를 CC BY-SA 4.0 으로
  연다. 상세는 `text/SOURCE.md`, `LICENSE`.
- ~~**O2. 현대어 유지 여부**~~ → **결정 (2026-07-25)**: 유지. 저본 번역이
  이미 담백하지만, `현대어`는 "더 쉬운 번역"이 아니라 **구어체 다리** 역할로
  쓴다. 문체는 `text/08.md` 를 기준 샘플로 삼는다.
- **O3. 원문 표기** — 한문만? 한글 독음 병기? 독음이 있으면 낭독은 좋은데
  파일이 길어진다.
- ~~**O4. 컨텍스트 크기**~~ → **측정 (2026-07-25)**: 81장 합계 약 117,000자.
  전량을 매 상담마다 읽기에는 무겁다. §3의 grep 검색을 유지한다. 다만
  프론트매터(`themes`/`situations`)만 모으면 훨씬 작으므로, 검색이 느려지면
  프론트매터만 뽑은 색인 파일을 만드는 선택지가 남아 있다.
- **O5. 다국어** — 상담은 한국어 고정으로 시작. 영어 지원은 나중에.
